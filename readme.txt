Task description:
https://github.com/BpCodingDojo/SongKata/blob/master/README.md

+ can return a number
+ can read header row from file
+ can check file open error
+ can interpret header
+ can detect if header is invalid
+ can interpret song lines
+ can detect if song lines are invalid
+ can return ordered hit list
